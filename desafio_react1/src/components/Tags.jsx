import Button from "react-bootstrap/esm/Button";

function Tags ({pinturaButton,textoButton}){

    return (
        
            <Button variant={pinturaButton}>{textoButton}</Button>
        
    )
};

export default Tags;